"""
src/analysis — statistical baselines and diagnostics for the slowdown CNN.

Modules
-------
baselines : scalar/logistic baselines (always-positive, random, year
            climatology, SIE(t), climate indices) evaluated on the same
            TVT splits as the CNN.

Import submodules directly; none of them require TensorFlow.
"""
